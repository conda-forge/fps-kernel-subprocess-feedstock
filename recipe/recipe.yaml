schema_version: 1

context:
  version: "0.2.4"

package:
  name: fps-kernel-subprocess
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/f/fps-kernel-subprocess/fps_kernel_subprocess-${{ version }}.tar.gz
    sha256: 8bc11064c6471c23583866a17af68d3f003736ffdf42b8d61c9d5692d77ced72

build:
  noarch: python
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - uv-build >=0.11.32,<0.12
  run:
    - python >=${{ python_min }}
    - anyio
    - fps
    - jupyverse-kernel >=0.1.0,<0.2.0
    - pyzmq
    - zmq-anyio >=0.3.14,<0.4.0

tests:
  - python:
      imports: fps_kernel_subprocess
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  license: BSD-3-Clause
  license_file: COPYING.md
  homepage: https://github.com/jupyter-server/jupyverse/tree/main/plugins/kernel_subprocess
  summary: An FPS plugin for the kernel subprocess API

extra:
  recipe-maintainers:
    - davidbrochart
